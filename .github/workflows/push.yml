name: Push to Replicate

# 'git push'를 하면 자동으로 실행되도록 변경
on:
  push:
    branches: [ "main" ]

jobs:
  push_to_replicate:
    runs-on: ubuntu-latest
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          docker-images: false

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Cog
        uses: replicate/setup-cog@v2
        
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Docker 레지스트리에 명시적 로그인 (Replicate 토큰 사용)
      - name: Docker Login to Replicate
        run: echo ${{ secrets.REPLICATE_API_TOKEN }} | docker login https://r8.im -u ${{ github.actor }} --password-stdin
        
      # 🌟🌟🌟 (수정) 1. 'cog build'와 'cog push'를 분리 🌟🌟🌟
      - name: Build Docker Image with HF_TOKEN
        # 🌟 'cog build'는 '--build-arg'를 지원합니다!
        # 🌟 HF_TOKEN Secret을 HF_TOKEN_BUILD_ARG라는 빌드 인자로 전달합니다.
        run: |
          cog build -t r8.im/sanghyeonpjeong/flux_capybara_lora \
            --build-arg HF_TOKEN_BUILD_ARG=${{ secrets.HF_TOKEN }}

      # 🌟🌟🌟 (수정) 2. 방금 빌드한 이미지를 푸시 🌟🌟🌟
      - name: Push Image to Replicate
        run: cog push r8.im/sanghyeonpjeong/flux_capybara_lora