name: Push to Replicate

on:
  workflow_dispatch:
    inputs:
      model_name:
        required: true
        description: 'username/modelname'

jobs:
  push_to_replicate:
    runs-on: ubuntu-latest
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          docker-images: false

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Cog
        uses: replicate/setup-cog@v2
        
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Docker 레지스트리에 명시적 로그인
      - name: Docker Login to Replicate
        run: echo ${{ secrets.REPLICATE_API_TOKEN }} | docker login https://r8.im -u ${{ github.actor }} --password-stdin
        
      # 🌟🌟🌟 (수정) --secret 플래그가 'env'를 읽도록 수정 🌟🌟🌟
      - name: Build and Push to Replicate
        # 1. 'env:'를 사용하여 GitHub Secret(HF_TOKEN)을 Runner의 환경 변수(HF_TOKEN_RUNNER_ENV)로 설정
        env:
          HF_TOKEN_RUNNER_ENV: ${{ secrets.HF_TOKEN }}
        # 2. --secret 플래그가 'src='(파일) 대신 'env='(환경 변수)를 읽도록 함
        # 3. 'id'는 'my_hf_secret'로 설정 (cog.yaml과 일치해야 함)
        run: cog push r8.im/${{ inputs.model_name }} --secret id=my_hf_secret,env=HF_TOKEN_RUNNER_ENV