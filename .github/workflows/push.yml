name: Push to Replicate

# 🌟🌟🌟 1. (수정) 'git push'를 하면 자동으로 실행되도록 변경 🌟🌟🌟
on:
  push:
    branches: [ "main" ]

jobs:
  push_to_replicate:
    runs-on: ubuntu-latest
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          docker-images: false

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Cog
        uses: replicate/setup-cog@v2
        
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Docker 레지스트리에 명시적 로그인
      - name: Docker Login to Replicate
        run: echo ${{ secrets.REPLICATE_API_TOKEN }} | docker login https://r8.im -u ${{ github.actor }} --password-stdin
        
      # 2. HF_TOKEN을 파일로 저장
      - name: Create HF_TOKEN secret file
        run: echo -n ${{ secrets.HF_TOKEN }} > hf_token_secret_file.txt
      
      # 🌟🌟🌟 3. (수정) model_name을 하드코딩 (입력창 제거) 🌟🌟🌟
      - name: Build and Push to Replicate
        # 'sanghyeonpjeong/flux_capybara_lora'가 모델 이름이 맞는지 확인하세요.
        run: cog push r8.im/sanghyeonpjeong/flux_capybara_lora --secret id=my_hf_secret,src=${{ github.workspace }}/hf_token_secret_file.txt