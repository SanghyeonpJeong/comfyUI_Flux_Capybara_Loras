name: Push to Replicate

# 'git push'를 하면 자동으로 실행되도록 변경
on:
  push:
    branches: [ "main" ]

jobs:
  push_to_replicate:
    runs-on: ubuntu-latest
    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@v1.3.1
        with:
          tool-cache: false
          docker-images: false

      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Cog
        uses: replicate/setup-cog@v2
        
      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Docker 레지스트리에 명시적 로그인
      - name: Docker Login to Replicate
        run: echo ${{ secrets.REPLICATE_API_TOKEN }} | docker login https://r8.im -u ${{ github.actor }} --password-stdin
        
      # 🌟 (수정) env:를 사용하여 HF_TOKEN을 '런타임'에 전달
      - name: Build and Push to Replicate
        env:
          # 이 HF_TOKEN을 predict.py의 os.environ.get()이 읽을 것입니다.
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        # 모델 이름을 하드코딩 (수동 입력창 제거)
        run: cog push r8.im/sanghyeonpjeong/flux_capybara_lora