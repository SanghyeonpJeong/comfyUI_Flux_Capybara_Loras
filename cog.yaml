build:
  gpu: true
  cuda: "12.1"
  python_version: "3.11" 
  python_packages:
    - "torch==2.1.2"
    - "diffusers==0.29.0"
    - "transformers==4.41.2"
    - "accelerate==0.30.1"
    # 🌟 predict.py가 snapshot_download를 사용하기 위해 [cli] 유지
    - "huggingface-hub[cli]"
    # 🌟 NumPy 1.x/2.x 충돌 해결을 위해 버전 강제
    - "numpy<2.0" 

  run:
    # 🌟 (수정) 22GB 모델 다운로드 코드를 '모두 삭제'
    
    # 2. LoRA 파일 다운로드 (이것만 남깁니다)
    - "mkdir -p /src/loras"
    - "curl -L -o /src/loras/Flux_Capybara_v1.safetensors https://github.com/SanghyeonpJeong/comfyUI_Flux_Capybara_Loras/raw/main/Flux_Capybara_v1.safetensors"
    
    # Git이 변경 사항을 인식하도록 강제 주석 추가
    # Force update v23

predict: "predict.py:Predictor"