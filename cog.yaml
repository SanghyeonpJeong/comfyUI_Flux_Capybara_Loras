build:
  python_version: "3.11"
  system_packages: []
  python_packages:
    - torch
    - torchvision
    # comfyui는 pip에서 설치가 따로 안 될 수 있으므로 별도 관리(아래 requirements.txt 일치)
  run:
    - pip install -r requirements.txt
