build:
  # 🌟 1. (수정) 'fofr/flux-dev'를 기반 이미지로 사용
  # 🌟 (수정) v28의 문법 오류 해결: 'base_image:' -> 'image:'
  # 🌟 (수정) v28의 문법 오류 해결: 'build:' 블록 '안'으로 이동
  image: "r8.im/fofr/flux-dev:latest" 

  # 🌟 2. (수정) NumPy 1.x/2.x 충돌 해결 (이것만 추가)
  python_packages:
    - "numpy<2.0" 

  run:
    # 🌟 3. (수정) 22GB 모델 다운로드 '삭제'
    
    # LoRA 파일 다운로드 (이것만 남깁니다)
    - "mkdir -p /src/loras"
    - "curl -L -o /src/loras/Flux_Capybara_v1.safetensors https://github.com/SanghyeonpJeong/comfyUI_Flux_Capybara_Loras/raw/main/Flux_Capybara_v1.safetensors"
    
    # Git이 변경 사항을 인식하도록 강제 주석 추가
    # Force update v29

predict: "predict.py:Predictor"