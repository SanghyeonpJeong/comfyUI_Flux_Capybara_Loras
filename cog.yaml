build:
  gpu: true
  cuda: "12.1"
  python_version: "3.11" 
  python_packages:
    - "torch==2.1.2"
    - "diffusers==0.29.0"
    - "transformers==4.41.2"
    - "accelerate==0.30.1"
    # 🌟 predict.py가 diffusers와 snapshot_download를 사용하기 위해 [cli] 유지
    - "huggingface-hub[cli]" 

  run:
    # 🌟 (수정) 22GB 모델 다운로드 코드를 '모두 삭제' (v21과 동일)
    
    # 2. LoRA 파일 다운로드 (이것만 남깁니다)
    - "mkdir -p /src/loras"
    - "curl -L -o /src/loras/Flux_Capybara_v1.safetensors https://github.com/SanghyeonpJeong/comfyUI_Flux_Capybara_Loras/raw/main/Flux_Capybara_v1.safetensors"
    
    # Git이 변경 사항을 인식하도록 강제 주석 추가
    # Force update v22

predict: "predict.py:Predictor"